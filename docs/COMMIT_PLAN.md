# Commit plan

One commit per row, in order. Each commit leaves `uv run ruff check . && uv run pytest -q` green (browser-marked tests may skip when Chrome is absent; they must pass when `BU_CDP_URL` points at a Chrome). Messages: English, conventional prefix, imperative, ≤ 72 chars, body only for a non-obvious why, no trailers.

A reference implementation of the loop exists outside the repo (jev-ultrafast clone, path in the brief). Adapt, do not paste; the history must read as an incremental build with tests in every commit.

| # | message | files | tests / verification |
|---|---|---|---|
| 1 | `chore: project skeleton with CLI entry point` | `pyproject.toml` (deps: `browser-harness[mcp]`, `httpx[http2]`; dev: pytest, ruff; script `jev-ra`; `browser` marker), `LICENSE` (MIT, Brian Kim, 2026), `THIRD_PARTY_NOTICES.md` (jev-ultrafast MIT notice), `.gitignore`, `README.md` (paragraph + WIP), `jev_ra/__init__.py`, `__main__.py`, `cli.py` (`--version` only), `docs/DESIGN.md`, `docs/COMMIT_PLAN.md` | `tests/test_cli.py::test_version`; `uv sync`; `uv run jev-ra --version` |
| 2 | `feat: config from environment and XDG config file` | `jev_ra/config.py` | `tests/test_config.py`: key precedence; `sk-or-` → OpenRouter; explicit endpoint wins; text model optional; viewport/budget defaults; invalid values fall back with warnings |
| 3 | `feat: Jev decisions client with strict answer validation` | `jev_ra/decide/__init__.py`, `decide/client.py` | `tests/test_client.py` (`httpx.MockTransport`): success; missing answer; choice not argmax; probabilities not summing; 429 retried once; 401 not retried; timeout → `JevUnavailable`; OpenRouter gets `session_id` |
| 4 | `feat: DOM snapshot script with identity and freshness guards` | `jev_ra/browser/__init__.py`, `browser/snapshot.js` (adapted; header notice), `tests/fixtures/form.html`, `list.html`, `autocomplete.html` | `tests/test_snapshot.py` (`browser`): serve fixtures on 127.0.0.1, evaluate snapshot through a raw CDP session, assert element roles/labels/refs, marker stable across a no-op, marker changes after typing, guard null for hidden element |
| 5 | `feat: browser session with trusted input and act-time guards` | `browser/session.py` | `tests/test_session.py` (`browser`): open fixture; click by ref changes marker; fill types via CDP; select picks option; `StalePage` when guard differs; covered element rejected; `press("Enter")` submits; screenshot returns JPEG bytes |
| 6 | `feat: action space builder for per-operation targets` | `browser/actions.py` | `tests/test_actions.py` (pure): elements deduped per node; fill+click for editable; select targets `idx:opt`; scroll/wait only when offered; 250 cap → `omitted` |
| 7 | `feat: decision policy with parallel questions` | `decide/questions.py`, `decide/policy.py` | `tests/test_policy.py`: questions only for offered ops; `value_for_field` only with values; `prev_ok` only with history; criteria all strings; `read_answers` validates ids; corrective re-ask removes invalid combo |
| 8 | `feat: value binding and optional text helper` | `jev_ra/text.py` | `tests/test_text.py`: host value chosen → used once; `none` + no helper → `needs_value`; helper JSON contract enforced; helper never called when values fit |
| 9 | `feat: agent loop with verification, loop detection and escalation` | `jev_ra/agent.py` | `tests/test_agent.py`: fake Session + fake decide; happy path to DONE; DONE without goal_achieved → continues then `unverified_done`; 3 no-change → `stuck_loop`; budget → `budget`; stale retried 3× then `stale`; Result fields; escalation candidates top-8 |
| 10 | `feat: DOM extraction for text, links, tables and main content` | `jev_ra/extract.py` | `tests/test_extract.py` (`browser`): fixtures with table/links/article; each mode; 20k cap + truncation flag |
| 11 | `feat: end-to-end run on local fixtures with a scripted decider` | `tests/test_e2e_fixtures.py` (`browser`), `tests/fixtures/checkout.html` | scripted `decide` walks fill → select → submit → DONE against real Chrome; ≤ 6 steps; typed values present on the result page |
| 12 | `feat: MCP server exposing the browser tools` | `jev_ra/mcp_server.py` | `tests/test_mcp_server.py`: tool list matches the DESIGN table; `browser_observe` through the MCP in-memory client with a fake Session; `ToolError` on closed session; `browser_run` returns `Result` JSON |
| 13 | `feat: CLI with persistent session, run, and single-step commands` | `jev_ra/cli.py` | `tests/test_cli_commands.py`: `run --json` prints Result; stateful commands read/write `session.json`; `close` removes it; `--value k=v` parsing |
| 14 | `feat: install helpers for Claude Code and Codex, doctor` | `cli.py` (`install`, `doctor`) | `tests/test_install_doctor.py`: install builds the exact `claude mcp add` / `codex mcp add` argv; missing binary → prints the command; key never printed; doctor without key exits 1; doctor with fake decide prints latency |
| 15 | `feat: bench with offline fixtures, live tasks and browser-use baseline` | `jev_ra/bench/` (tasks = the three DESIGN tasks; baseline rows loaded from `docs/benchmarks/2026-09-18-browser-use-baseline/*.jsonl`), `cli.py` (`bench`), `docs/benchmarks/2026-09-18-browser-use-baseline/` (already in the tree, commit it here) | `tests/test_bench.py`: offline bench runs fixture tasks with the scripted decider and prints ms/steps; baseline loader parses the jsonl rows and computes the ratio table; `--live` gated on key + Chrome and prints `jev-ra ms / flash_mode ms = ratio` per task plus PASS/FAIL against the ≥ 3× acceptance |
| 16 | `docs: README with quick start, MCP setup, benchmarks and limits` | `README.md`, `README.ko.md`, `CHANGELOG.md` | proofread against `uv run jev-ra --help` and the MCP tool list; measured numbers only, with date and route |
| 17 | `ci: ruff and pytest with headless Chrome` | `.github/workflows/ci.yml` (uv; Python 3.12-3.14; install Chrome; start with `--remote-debugging-port` on a temp profile; export `BU_CDP_URL`) | workflow parses; browser tests are not skipped in CI |

Stop after 17. Do not push. Do not tag. Report `git log --oneline`, the last test run verbatim, `uv run jev-ra bench` (offline) output, and `uv run jev-ra install claude --scope user` output with no key in the environment.
